{
  "buildCommand": "cd ../.. && npm install && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/:path*", "destination": "https://khora-server.fly.dev/api/:path*" },
    { "source": "/ws", "destination": "https://khora-server.fly.dev/ws" }
  ]
}
